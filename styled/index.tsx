import {SafeAreaView} from 'react-native';
import styled from 'styled-components';

export const ScreenCenteredView = styled(SafeAreaView)`
  flex: 1;
  align-items: center;
  justify-content: center;
`;
